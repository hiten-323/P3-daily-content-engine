# Purity Beans — Autonomous Growth Engine
# Install: pip install -r requirements.txt
#
# Core (always required)
requests>=2.31.0

# LLM providers (install at least one)
google-generativeai>=0.7.0      # Gemini Flash — primary LLM
groq>=0.9.0                     # Groq Llama — fallback LLM
openai>=1.30.0                  # OpenRouter (uses OpenAI SDK) — final fallback

# Scheduling
apscheduler>=3.10.0             # Daemon mode (not needed for GitHub Actions)

# Optional: semantic deduplication (recommended)
sentence-transformers>=2.7.0   # Best dedup quality
scikit-learn>=1.4.0             # TF-IDF fallback

# Optional: trend & competitor research
pytrends>=4.9.2                 # Google Trends
praw>=7.7.0                     # Reddit research

# Optional: JSON repair (graceful LLM output parsing)
json-repair>=0.28.0

# Optional: image generation
Pillow>=10.3.0                  # Carousel rendering + branded placeholder (always works)
# Primary image provider: Hugging Face Inference API — FREE with a free HF account token
#   Signup: huggingface.co → Settings → Access Tokens (read token is enough)
#   GitHub Secret: HF_TOKEN
#   Model: FLUX.1-schnell (~1,000 free images/month)
#   Uses stdlib urllib only — no extra package needed.
#
# Fallback chain: HuggingFace → Pollinations (anonymous, region-dependent) → fal.ai (paid)
# fal-client is only needed if you want fal.ai as a paid fallback:
# fal-client>=0.4.0             # Flux AI paid fallback (set FAL_KEY / FLUX_API_KEY)

# Optional: video generation
moviepy>=2.2.1                  # YouTube Shorts slideshow fallback
runwayml>=0.1.0                 # Runway Gen-3 video (requires RUNWAY_API_KEY)
pydantic>=2.0.0                 # Schema validation
langdetect>=1.0.9               # Language detection

